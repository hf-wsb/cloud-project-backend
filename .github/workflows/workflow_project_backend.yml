name: Hello World
on: 
  push:
    branches: ["master"]
  pull_request:
    brnaches: ['master']
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Print Hello World
      run: python -c "print('Hello, world!')"